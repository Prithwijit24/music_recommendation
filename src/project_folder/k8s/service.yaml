apiVersion: v1
kind: Service
metadata:
  name: music-recommendation-service
spec:
  selector:
    app: music-recommendation-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
  type: ClusterIP
